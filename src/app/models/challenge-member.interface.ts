export interface ChallengeMember {
    id: string,
    isAccepted: boolean, // true = accepted, false = pending
    isCompleted: boolean, // true = accepted, false = pending
    score: number, // true = accepted, false = pending
}

